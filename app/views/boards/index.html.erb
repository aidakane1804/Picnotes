<%= render './partials/user_dashboard' %>

<%= render './modals/new_board_modal' %>

<div class = "notes-container">
  <div class = 'note-container'>
    <div class = 'board-spacer'>
    </div>
    <button
      type = 'button'
      class = 'no-look-btn'
      data-toggle = 'modal'
      data-target = '#newBoardModal'
      data-whatever = '@mdo'>
      New Board
    </button>
  </div>
  <% @boards.each do |board| %>
    <div class = 'note-container'>
      <div class = 'image-container'>
        <% board.notes.each_with_index do |note ,i| %>
          <% if i < 4 %>
            <div class = 'image-container-individual'>
              <%= image_tag note.image.url(:board_thumb), class: 'board-image' if note.image.present? %>
            </div>
          <% end %>
        <% end %>
      </div>
      <p class = 'no-look-btn'> <%= link_to ("#{board.name}"), user_board_path(@user, board), class: 'no-look-btn' %> </p>
    </div>
  <% end %>
</div>
