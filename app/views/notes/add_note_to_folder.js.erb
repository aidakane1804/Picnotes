
$('#profileDetails').append("<%= j render 'add_note_to_folder', note: @note, folders: @folders %>");
$('#noteContent').html("<%= j render 'add_note_to_folder', note: @note, folders: @folders %>");
$('#searchDetails').html("<%= j render 'add_note_to_folder', note: @note, folders: @folders %>");

setTimeout(function(){
 $('#add_note_to_folder').modal('show');
}, 500);

$('#loader').addClass('hidden');
