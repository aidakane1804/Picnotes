if($('#picnote-modal').length)
  $('#picnote-modal').remove();

setTimeout(function(){
  $('#profileDetails').append("<%= j render 'note_modal', note: @note %>");
  $('#noteContent').html("<%= j render 'note_modal', note: @note %>");
}, 300);

setTimeout(function(){
 if($('#test-modal').length){
  $('#test-modal').modal('show');
 }
}, 500);

$('#loader').addClass('hidden');