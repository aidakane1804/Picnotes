debugger
$('#notes-div').append('<%= j render 'notes/note' %>');
<% if @notes.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @notes %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>